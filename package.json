{
  "name": "open-awair",
  "version": "0.1.0",
  "description": "Open-source framework for Awair custom firmware and host-side tooling.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "typecheck": "tsc -p host/tsconfig.json --noEmit",
    "lint": "tsc -p host/tsconfig.lint.json --noEmit",
    "test": "tsc -p host/tsconfig.json --outDir dist --module CommonJS && node --test dist/test/*.js",
    "migration:build": "tsc -p tools/migration/tsconfig.json",
    "migration:test": "npm run migration:build && node --test tools/migration/test/*.test.mjs",
    "migration:serve": "npm run migration:build && node tools/migration/cli.mjs serve",
    "migration": "node tools/migration/cli.mjs",
    "test:all": "npm test && npm run migration:test"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
